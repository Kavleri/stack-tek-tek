# AI Context Handover - Dream Syariah Wedding

**To AI Assistant:**
If you are reading this file, you have been hooked into an ongoing session to assist with the "Dream Syariah Wedding" project (Fullstack). Below is the current architecture and state of the repository.

## 1. Project Architecture Decisions
- **Paradigm**: Decoupled Multi-Page Application (Frontend, Backend, Database are separated).
- **Frontend**: `frontend/` directory. Built with React.js using Vite (TypeScript).
- **Backend**: `backend/` directory. Built with Node.js and Express.js (TypeScript configured via ts-node-dev).
- **Database**: `database/` directory. Target engine: PostgreSQL hosted on **Supabase**. (Currently awaiting team approval for DDL schema, DO NOT implement random tables without consulting the user).

## 2. Completed Milestones
- [x] Converted old monolithic repo into a 3-pillar decoupled architecture (`frontend/`, `backend/`, `database/`).
- [x] Initialized Vite-React in frontend.
- [x] Initialized Express dependencies in backend (cors, helmet, morgan, pg, dotenv, typescript).
- [x] Cleared `database/schema.sql` and marked it as pending team approval for Supabase ORM (Prisma/Drizzle) integration.
- [x] Rewrote Git contribution history to attribute all commits strictly to `@Kavleri` and purged unwanted system committers. Force-pushed to origin `main`.

## 3. Next Logical Steps (Pending User Command)
1. **Frontend**: Setup Tailwind CSS v4, implement responsive UI routing (Dashboard/Form).
2. **Backend**: Setup entry-point `src/index.ts`, CORS config to accept frontend URL, routes initialization.
3. **Database**: Implement `Prisma` or `Drizzle` inside the Backend once the team agrees on the ERD (table relationships).

**System Note:** Obey the user's PRD specs located in `doc/prd.md` before generating new code. Maintain a premium "Navy & Gold" theme constraint on the frontend.
